名称：字体
文件：font


相对上一个版本：
无


当前版本：

【V0.9.0】

--简单的加载和打印功能

====================
--初始化字体系统

initFont()

初始化字体系统

====================
--添加字体

addFont(string,string)

添加一个字体文件
[注释：将创建1到32号字体大小]

====================
--打印文字

printf(string,int,int,string,int,table)

使用指定的颜色打印指定大小指定字体的字符串到屏幕上的指定位置
[注释：参数：打印的字符串，坐标，坐标，字体名称，字体大小，字体颜色]

额外注释：
====================
无